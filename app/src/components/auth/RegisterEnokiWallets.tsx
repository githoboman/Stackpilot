/**
 * Enoki / Sui zkLogin wallet registration — REMOVED on Stackpilot.
 *
 * The Stackpilot base offered Google sign-in via Sui's Enoki zkLogin. Stackpilot uses
 * Stacks wallets (Leather / Xverse) only, so this is now an inert no-op kept solely
 * so any stale import resolves. It is no longer mounted in main.tsx.
 */
export function RegisterEnokiWallets() {
  return null;
}
